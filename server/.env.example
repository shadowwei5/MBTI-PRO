# MBTI-PRO 生产环境变量
# 复制此文件为 .env 并填入实际值，或在 Render/Vercel 面板中直接设置

# 必填：火山引擎 API Key（图片生成用，日常运行不需要）
VOLCENGINE_API_KEY=ark-xxxxxxxxxx

# 必填：XorPay 支付配置（去 xorpay.com 后台 -> 支付方式 -> 找到 aid 和 app_secret）
XORPAY_AID=
XORPAY_SECRET=
# 支付回调地址，必须是公网可达的 URL（生产环境用 https://mbti-pro.com/api/payment/callback）
XORPAY_NOTIFY_URL=

# 可选：SMTP 邮件发送配置（用于支付成功后自动发送报告邮件）
# QQ邮箱需开启SMTP服务并获取授权码：设置 → 账户 → POP3/SMTP服务
SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_USER=your-email@qq.com
SMTP_PASS=your-smtp-auth-code

# 可选：生产域名（CORS 白名单）
CLIENT_ORIGIN=https://mbti-pro.vercel.app

# 以下均已有默认值，通常无需修改
# VOLCENGINE_BASE_URL=https://ark.cn-beijing.volces.com
# VOLCENGINE_IMAGE_MODEL=doubao-seedream-5-0-lite-260128
# PORT=3001
